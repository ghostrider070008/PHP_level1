//Задание 3
<?php
    $a = 5;  // переменной a присваивем значение 5						
    $b = '05'; // переменной b присваивем значение строки 05
    var_dump($a == $b);         // будет true потому что и там и там будет 5 без сравнения типов переменных
    var_dump((int)'012345');     // Почему 12345? 		// потому что мы приводим значение переменной к целочисленному значению 12345, 0 не будет учитываться
    var_dump((float)123.0 === (int)123.0); // Почему false?  // потому что сравнивается еще и типы данных, а они различные (float и integer)
    var_dump((int)0 === (int)'hello, world'); // Почему true?  //строка не ноль, значит ее значение - true
?>
